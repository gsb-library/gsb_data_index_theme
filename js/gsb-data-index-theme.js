/**
 * @file
 * GSB Data Index Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.gsbDataIndexTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
